version: '3'

services:

  mumble:
    build:
      context: .
      dockerfile: Dockerfile
    image: mhilker/murmur
    volumes:
      - ./data/:/var/lib/murmur/
    ports:
      - 64738:64738
      - 64738:64738/udp
